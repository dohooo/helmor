{
	"name": "@helmor/cloud",
	"description": "Helmor Team Cloud Sandbox — Cloudflare Worker control plane (Phase 0).",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@cloudflare/sandbox": "0.12.1"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20260609.0",
		"typescript": "^5.7.0",
		"wrangler": "^4.0.0"
	}
}
