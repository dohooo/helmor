{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "voice-panel",
	"description": "Capability for the desktop voice-panel webview. Needs the same core IPC as the main window (settings load, voice tools, diag logging, cross-window event listen/emit) but is scoped narrowly to the `voice-panel` label so the main-window capability stays focused on its own surface.",
	"windows": ["voice-panel"],
	"permissions": [
		"core:default",
		"core:event:default",
		"core:window:allow-start-dragging"
	]
}
