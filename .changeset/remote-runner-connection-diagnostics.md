---
"helmor": minor
---

Settings → Runtime Debug gains a Connection diagnostics panel that surfaces RPC pipe telemetry, server-reported health, agent-session count, fresh ping RTT, and the close reason (if the pipe went away). The desktop's operator-facing "is my remote OK?" tool: cumulative I/O counters, decode errors, and uptime all visible in one card without reaching for log files.
