---
"helmor": minor
---

Workspaces pinned to a remote runtime now drive the inspector's file tree, diff viewer, and stage/unstage/discard actions over the SSH-backed JSON-RPC seam — no per-feature branching, same UI as a local workspace.
