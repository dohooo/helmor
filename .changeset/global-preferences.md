---
"helmor": minor
---

Settings has a new "Global preferences" section that holds template prompts for every repo. Each per-repo preference field can either follow the global value or be overridden — typing into a repo field automatically detaches it, and a "Reset to global" link reattaches. Editing a global field propagates to every repo that follows it, with a toast confirming how many were affected.
