---
"helmor": minor
---

Settings → Runtime Debug gains a "Remote agent sessions" panel that lists every in-flight agent turn the daemon knows about and exposes per-session Abort and Reattach controls — useful for cleaning up orphaned sessions left over from a desktop that crashed mid-stream.
