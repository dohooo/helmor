---
"helmor": minor
---

New `Cmd+Shift+S` workspace search panel: an overlay with a debounced search input, case-insensitive and fixed-string toggles, keyboard-navigable result list, and click-to-open-at-line. Local workspaces grep in-process; remote-bound workspaces dispatch over SSH through `workspace.search`.
