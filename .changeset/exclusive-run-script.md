---
"helmor": patch
---

Add a per-repo Exclusive toggle on the run script that makes starting a new run stop any other live run in the same repository first — useful when the script binds a fixed port.
