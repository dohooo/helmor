---
"helmor": minor
---

Add MiMo Code (Xiaomi's MiMo-Code) as a built-in agent — sign in with `mimo auth login`, pick MiMo models in the composer, and manage custom providers in Settings, just like OpenCode.
