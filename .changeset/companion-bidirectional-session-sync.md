---
"helmor": patch
---

Fix live session sync so that a message sent from your phone (or another window) and the agent's response now appear in real time on every open view of that session, in both directions, instead of only after a reload.
