---
"helmor": minor
---

Workspaces can now be pinned to a specific remote runtime: the Add Workspace dialog gains a "Where" picker that defaults to local but lets you choose any registered remote, the binding persists in a new `workspaces.runtime_name` column, and the workspace list surfaces the runtime as a chip so remote-bound workspaces are visually distinct from local ones.
