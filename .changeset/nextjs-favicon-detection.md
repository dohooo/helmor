---
"helmor": patch
---

Detect favicons for Next.js projects that use the `src/app/` layout (and a few other missed framework conventions like `static/favicon.svg`, `app/icon.svg`, and `apple-icon.*`) so their workspace avatar matches the project icon instead of falling back to initials.
