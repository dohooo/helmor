---
"helmor": patch
---

Team cloud: the headless serve container image no longer fails to build on Linux — the Kimi Code CLI (which ships no Linux release) is now skipped there, like the other desktop-only agents, instead of erroring on an unextractable archive.
